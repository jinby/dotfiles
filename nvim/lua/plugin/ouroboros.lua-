return{
    'jakemason/ouroboros',
    dependencies = {
        'nvim-lua/plenary.nvim'
    },
    -- config = function()
    --   require("ouroboros").setup({
    --         
    --         })
    -- end,
}
